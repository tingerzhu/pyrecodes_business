# Direct dependencies only - pip resolves the rest automatically.
#
# Mac (without conda):
#   brew install hdf5          # needed for pandana and tables
#   python3.9 -m venv env
#   source env/bin/activate
#   pip install -r requirements.txt
#   pip install -e .
#
# Linux:
#   sudo apt install libhdf5-dev   # or: conda install hdf5
#   python3.9 -m venv env
#   source env/bin/activate
#   pip install -r requirements.txt
#   pip install -e .

# Core scientific
numpy>=1.26,<2
pandas>=2.2,<3
scipy>=1.13,<2
scikit-learn>=1.6,<2
matplotlib>=3.9,<4
networkx>=3.2,<4
imageio>=2.37
openpyxl>=3.1

# Geospatial (ship self-contained wheels on Mac/Linux - no GDAL install needed)
geopandas>=1.0,<2
shapely>=2.0,<3
pyproj>=3.6,<4
rasterio>=1.4,<2
fiona>=1.10,<2
contextily>=1.6

# Requires HDF5 system library (see note above)
pandana>=0.7
tables>=3.9

# Water network simulation
rewet==0.2.0b12
wntrfr==1.1.0.1.2

# Jupyter
ipykernel>=6.29
ipython>=8.18
